"use client";
import React from 'react';

const LikeButton = () => {
    return (
        <div>
           <button onClick={() => alert("Hello World!")}>Click Me</button> 
        </div>
    );
};

export default LikeButton;